# 🎴 메모리 카드 게임

가장 간단한 웹 기술(HTML, CSS, JavaScript)로 만든 카드 뒤집기 게임입니다. Supabase를 활용한 실시간 리더보드와 점수 저장 기능을 제공합니다.

## ✨ 주요 기능

### 🎮 게임 기능
- **3가지 난이도**: 쉬움(3x4), 보통(4x4), 어려움(4x6)
- **실시간 타이머**: 게임 진행 시간 측정
- **점수 시스템**: 시간과 효율성을 고려한 점수 계산
- **힌트 시스템**: 매칭 가능한 카드 쌍 표시
- **일시정지/재개**: 게임 중단 및 재시작
- **카드 뒤집기 애니메이션**: 부드러운 3D 효과

### 🏆 리더보드
- **실시간 랭킹**: Supabase를 통한 실시간 점수 동기화
- **레벨별 필터링**: 난이도별 순위 확인
- **오프라인 지원**: 네트워크 연결 없이도 로컬 기록 유지
- **자동 동기화**: 온라인 복구시 오프라인 데이터 동기화

### 💾 데이터 관리
- **Supabase 연동**: 안전한 클라우드 점수 저장
- **로컬 백업**: 오프라인 상황 대비 로컬 스토리지 활용
- **플레이어 설정**: 이름, 난이도 등 개인 설정 저장

## 🚀 시작하기

### 1. 프로젝트 설정
```bash
# 프로젝트 클론 또는 다운로드
cd card-game

# 로컬 서버 실행 (예: Live Server 확장 프로그램 사용)
# 또는 Python HTTP 서버
python -m http.server 8000
```

### 2. 환경 변수 설정 (보안)

**개발 환경:**
1. `.env.example` 파일을 `.env`로 복사:
```bash
cp .env.example .env
```

2. `.env` 파일에 실제 Supabase 정보 입력:
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

**배포 환경 (Vercel):**
1. Vercel 대시보드에서 프로젝트 선택
2. Settings → Environment Variables에서 추가:
   - `SUPABASE_URL`: `https://your-project.supabase.co`
   - `SUPABASE_ANON_KEY`: `your_supabase_anon_key_here`

> ⚠️ **보안 주의사항**: 
> - `.env` 파일은 절대 Git에 커밋하지 마세요
> - API 키는 환경 변수로만 관리하세요
> - 로컬 개발시에만 기본값이 사용됩니다

### 3. Supabase 설정
1. [Supabase](https://supabase.com)에서 새 프로젝트 생성
2. 환경 변수 또는 배포 플랫폼에서 설정 정보 관리

### 3. 데이터베이스 테이블 생성
Supabase SQL 편집기에서 다음 쿼리 실행:
```sql
CREATE TABLE card_game_scores (
    id BIGSERIAL PRIMARY KEY,
    player_name VARCHAR(50) NOT NULL,
    score INTEGER NOT NULL,
    level INTEGER NOT NULL,
    cards_flipped INTEGER NOT NULL,
    time_taken INTEGER NOT NULL,
    completed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## 🎯 게임 플레이 방법

1. **게임 설정**
   - 플레이어 이름 입력 (선택사항)
   - 난이도 선택 (쉬움/보통/어려움)
   - "게임 시작" 버튼 클릭

2. **게임 진행**
   - 카드를 클릭하여 뒤집기
   - 같은 그림의 카드 2장을 맞추기
   - 모든 카드를 매칭하여 게임 완료

3. **점수 계산**
   - 기본 점수 + 시간 보너스 + 효율성 보너스
   - 빠른 시간과 적은 시도로 완료할수록 높은 점수

## ⌨️ 키보드 단축키

- **스페이스바**: 게임 일시정지/재개
- **H**: 힌트 표시
- **R**: 게임 다시 시작
- **ESC**: 모달 창 닫기

## 📱 반응형 디자인

모바일, 태블릿, 데스크톱 모든 기기에서 최적화된 게임 경험을 제공합니다.

## 🔧 기술 스택

### 프론트엔드
- **HTML5**: 시맨틱 마크업
- **CSS3**: Grid 레이아웃, Flexbox, CSS 변수, 애니메이션
- **Vanilla JavaScript**: ES6+ 기능 활용
- **Web APIs**: Local Storage, Audio Context

### 백엔드
- **Supabase**: PostgreSQL 데이터베이스, 실시간 기능
- **REST API**: 점수 저장/조회 API

## 📁 프로젝트 구조

```
card-game/
├── index.html              # 메인 HTML 파일
├── styles/
│   ├── main.css           # 기본 스타일 및 컴포넌트
│   ├── game.css           # 게임 화면 전용 스타일
│   └── animations.css     # 애니메이션 및 효과
├── scripts/
│   ├── config.js          # 설정 및 상수
│   ├── database.js        # Supabase 데이터베이스 연동
│   ├── game.js           # 게임 로직 (CardGame 클래스)
│   └── main.js           # 앱 초기화 및 이벤트 처리
├── assets/
│   └── images/
│       └── cards/        # 카드 이미지 (현재는 이모지 사용)
└── README.md
```

## 🎨 커스터마이징

### 카드 테마 변경
`scripts/config.js`의 `CARD_DATA` 객체를 수정하여 새로운 카드 테마 추가:
```javascript
const CARD_DATA = {
    animals: ['🐶', '🐱', '🐭', ...],
    newTheme: ['🌟', '⭐', '✨', ...] // 새 테마 추가
};
```

### 난이도 조정
`GAME_LEVELS` 객체에서 그리드 크기, 시간 제한, 점수 등 조정:
```javascript
const GAME_LEVELS = {
    4: { // 새 난이도 추가
        name: "매우 어려움",
        grid: { rows: 6, cols: 6 },
        timeLimit: 300,
        baseScore: 2000
    }
};
```

### 스타일 커스터마이징
`styles/main.css`의 CSS 변수를 수정하여 색상 및 테마 변경:
```css
:root {
    --primary-color: #your-color;
    --secondary-color: #your-color;
    /* ... */
}
```

## 🔍 디버깅

개발자 모드가 활성화되어 있어 콘솔에서 다양한 정보를 확인할 수 있습니다:
- 게임 상태 로그
- 데이터베이스 연결 상태
- 점수 계산 과정
- 오류 메시지

## 🚀 배포 방법

### GitHub Pages
1. GitHub 저장소에 코드 업로드
2. Settings > Pages에서 소스 브랜치 설정
3. Supabase URL과 Key를 환경변수로 설정

### Netlify
1. 프로젝트를 Netlify에 연결
2. 빌드 설정 없이 정적 사이트로 배포
3. 환경변수에 Supabase 설정 추가

### Vercel
```bash
npm install -g vercel
vercel --prod
```

## 🐛 알려진 이슈

- Safari에서 일부 CSS 애니메이션 성능 최적화 필요
- 모바일 Safari에서 사운드 재생시 사용자 상호작용 필요

## 🤝 기여하기

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📝 라이선스

MIT License - 자세한 내용은 LICENSE 파일을 참조하세요.

## 🙏 감사의 말

- [Supabase](https://supabase.com) - 훌륭한 백엔드 서비스
- [Google Fonts](https://fonts.google.com) - Noto Sans KR 폰트
- 오픈소스 커뮤니티의 모든 기여자들

---

⭐ 이 프로젝트가 도움이 되었다면 별표를 눌러주세요!

📧 문의사항이 있으시면 이슈를 등록해 주세요.